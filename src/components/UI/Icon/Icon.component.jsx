// Import: Dependencies
import React from "react";
import styled from "styled-components";

// UI: Icon
function Icon({ icon }) {
  return <i className={icon}></i>;
}

// Export: Icon
export default Icon;
